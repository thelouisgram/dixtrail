/** Shared React Query timing — keeps polling from hammering the server. */
export const NOTIFICATIONS_REFETCH_MS = 120_000;
export const SEARCH_DEBOUNCE_MS = 400;
export const CITY_SEARCH_DEBOUNCE_MS = 300;
